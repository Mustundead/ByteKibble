# ByteKibble hand-drawn installer artwork

User-selected 2026-09-12 after rejecting generic ribbon, busy data paths, blank/noisy backgrounds and textured 3D kibble. The latest explicit selection controls the visual treatment.

- Generator: built-in ImageGen (not CLI/API fallback).
- Selected source: `installer-handdrawn-source.png`, copied from generated `exec-66d1eaef-64fe-4376-a617-7ced4db18c40.png`.
- `installer.png`: mechanically resized to 720 × 480 for Finder (no creative redraw).
- App resource `WelcomeArtwork.png` is the separate approved bowl/byte cutout, with user-authorized deterministic alpha cleanup. `scripts/clean-welcome-alpha.py` records its mask; the pre-mask original is retained here. The welcome layout is native SwiftUI, with the supplied MU LABS wordmark at 96pt width.
- Native Finder overlays the real signed app and Applications shortcut. The background contains no fake app or folder icons.
- Prior generated versions remain historical, not current installer assets.

## Final generation prompt

Use the provided ByteKibble installer layout as composition reference. REDRAW EVERYTHING as a clean charming HAND-DRAWN 2D editorial illustration, NOT 3D. Landscape 1536x1024. Preserve two large EMPTY rounded-square icon placement panels left and right in upper-middle, a simple right-pointing arrow between, and a small cat kibble bowl plus one rounded-square paw-stamped kibble byte in the lower-right. Use warm deep-brown smooth hand-inked outlines with rounded ends and subtle natural line variation; flat buttery cream and honey-gold fills, at most one simple flat shadow shape per object. Cozy thoughtful modern hand drawing, clean digital ink, no hatching, no sketch construction marks, no watercolor bleeding, NO paper texture, NO grain, NO noise, NO stippling, NO speckles. The paw symbols should have exactly three oval toes above one round pad. Keep two icon panels EMPTY, just warm ivory fill and an airy hand-drawn contour, real Finder icons will be overlaid later. Middle arrow is bold warm amber with hand-ink contour, clearly left to right. Background pale cream with only 3 or 4 very large faint rounded pixel blocks at outer edges, plenty of calm breathing room. Bowl and kibble charming but subordinate to installation path. No text, no letters, no actual app/folder icons, no screenshot/window border, no logos other than paw, no complex data streams, no extra objects. Strongly and unmistakably hand-drawn flat illustration, not glossy 3D or photoreal. Preview variant.
